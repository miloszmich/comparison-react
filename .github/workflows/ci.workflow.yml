name: Deploy CI
on:
  push:
    branches:
      - main

jobs:
  build-test-deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
      - name: Set-up Node
        uses: actions/setup-node@v1
        with:
          key: ${{ secrets.SSH_KEY }}
          node-version: "10.x"
      - run: yarn install
      - run: yarn build
      - name: Deploy
        uses: hendrysiak/comparison-react
        with:
          target_branch: gh-pages
          build_dir: build
        env:
          GITHUB_TOKEN: ${{ secrets.REPO_TOKEN }}